import React from "react";
import { View, Text, StyleSheet, Button } from "react-native";

const Track = (props) => {
  return (
    <View>
      <Text>this is the tracking page</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default Track;
