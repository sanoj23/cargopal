import React from "react";
import { View, Text, StyleSheet, Button } from "react-native";

const Support = (props) => {
  return (
    <View>
      <Text>this is the Supporting page</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default Support;
